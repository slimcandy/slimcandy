export type SeoPropsType = {
  title?: string
  description?: string
  path?: string
}

export type SiteMetaDataType = {
  site?: {
    siteMetadata?: {
      title: string
      description: string
      siteUrl: string
    }
  }
}
